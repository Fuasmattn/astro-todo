---
import Layout from "../layouts/Layout.astro";
import Sidebar from "../components/Sidebar.astro";
import type { List } from "./[listId].json";

const response = await fetch("https://randomuser.me/api/");
const data = await response.json();
const randomUser = data.results[0];
---

<Layout title="WeAstro">
  <main class="flex items-center justify-between">
    <div class="w-1/2 mx-auto">
      <h1 class="text-8xl">Hello {randomUser.name.first}</h1>
      <h2 class="mt-4 text-2xl opacity-80">
        This is WeAstro, the place where tasks are managed. The random name is
        fetched at build time using the <span class="text-pink-500"
          >global <code>fetch()</code> function</span
        >. In a dev build it's updating on page refresh.
      </h2>
      <div class="mt-8 flex gap-4">
        <a
          class="rounded-full border-2 border-neutral-50 bg-transparent px-6 py-2 text-base font-semibold uppercase hover:border-pink-600 hover:text-pink-600"
          href="/lists/create">+ Create New List</a
        >
      </div>
    </div>
  </main>
</Layout>
